import assert from 'assert';
import nodeWeixinDesktop from '../lib';

describe('node-weixin-desktop', function () {
  it('include nodeWeixinDesktop', function () {
    assert(nodeWeixinDesktop);
    console.log(nodeWeixinDesktop);
  });
});
